{
  "name": "latent-sky-web",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "Latent Sky viewer — CesiumWidget, zero ion, static files only.",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "check": "svelte-check --tsconfig ./tsconfig.json",
    "fixture": "python dev-fixture/generate.py",
    "test": "node tests/smoke.spec.mjs"
  },
  "dependencies": {
    "cesium": "1.143.0"
  },
  "devDependencies": {
    "@sveltejs/vite-plugin-svelte": "^7.2.0",
    "playwright-core": "^1.62.1",
    "svelte": "^5.56.8",
    "svelte-check": "^4.7.4",
    "typescript": "^5.9.0",
    "vite": "^8.2.0",
    "vite-plugin-static-copy": "^4.1.1"
  }
}
